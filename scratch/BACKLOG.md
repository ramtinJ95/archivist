- README follow-up: consider adding implementation-status section or feature matrix once compatibility milestones are formally verified.
- Platform follow-up: editor/pager launching currently depends on `sh -c`, so Windows is not a supported release target until command execution is abstracted or platform-specific launch paths are added.
- Launch/readiness follow-up: README currently positions tagged binaries and `go install ...@vX.Y.Z` as available install paths, but `git ls-remote --tags origin` returns no tags and local source builds report `archivist dev`, which makes the readiness message ahead of the published release state. Decide whether to cut an initial tagged release or soften the docs until one exists. Files: `README.md` lines 20-27 and 36-52, `internal/cli/version.go` lines 9-17, `.github/workflows/release.yml` lines 3-35.
- Plan-alignment follow-up: the implementation plan describes richer TUI chooser/export flows and broader validation coverage, but the current TUI relies on free-form text-input wizards and in-memory generate preview only, while validation does not yet report ambiguous refs or broader malformed filename cases. Decide whether to implement the remaining UX/validation scope or trim the plan to match the shipped surface. Files: `internal/tui/tui.go` lines 179-249, `internal/tui/wizard.go` lines 63-85 and 244-260, `internal/adrlog/validate.go` lines 9-110.
- TUI search follow-up: the plan calls for search/filter over titles and optionally contents, but the current Bubble Tea list filter only indexes `ADRItem.FilterValue()` and that returns title only, so body/content search is not available in the interactive workflow. Files: `internal/tui/item.go` lines 22-23, `internal/tui/tui.go` lines 55-59.
- TUI generate preview bug: `updateLayout()` always calls `updatePreview()`, which resets the shared viewport to the selected ADR content, so generated TOC/graph detail views are overwritten immediately after `updateGenerateView()` sets them. Files: `internal/tui/tui.go` lines 147-170 and 313-342.
