- README follow-up: consider adding implementation-status section or feature matrix once compatibility milestones are formally verified.
- Platform follow-up: editor/pager launching currently depends on `sh -c`, so Windows is not a supported release target until command execution is abstracted or platform-specific launch paths are added.
- Launch/readiness follow-up: README currently positions tagged binaries and `go install ...@vX.Y.Z` as available install paths, but `git ls-remote --tags origin` returns no tags and local source builds report `archivist dev`, which makes the readiness message ahead of the published release state. Decide whether to cut an initial tagged release or soften the docs until one exists. Files: `README.md` lines 20-27 and 36-52, `internal/cli/version.go` lines 9-17, `.github/workflows/release.yml` lines 3-35.
- Plan-alignment follow-up: the implementation plan describes richer TUI chooser/export flows, but the current TUI intentionally uses lightweight free-form wizards and in-memory generate previews. Decide whether to implement the remaining UX scope or trim the plan to match the shipped surface. Files: `internal/tui/tui.go`, `internal/tui/wizard.go`.
